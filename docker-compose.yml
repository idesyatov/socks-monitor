services:
  app:
    build: .
    ports:
      - "${BIND:-0.0.0.0}:${PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/dashboard.db
    restart: unless-stopped
